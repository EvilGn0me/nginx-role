server {
    listen       80;
    server_name  {{ item.value.hostname }};

    {% if item.value.stub == True %}
    location /stub_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        deny all;
    }
    {% endif %}
}
